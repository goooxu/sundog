# render outputs
out/

# large assets (provenance in assets/LICENSES.md); meshes are committed
# in-tree, only the 20 MB sky HDRI stays download-on-demand
assets/*.obj
assets/*.hdr
!assets/sparky.obj
!assets/spot.obj
!assets/capsule_mascot.obj

# build trees (dev box; on the test box builds go to /tmp/sundog-build)
build*/

# per-render statistics
*.stats.json

# python bytecode (scenes/ scripts import scenelib)
__pycache__/
